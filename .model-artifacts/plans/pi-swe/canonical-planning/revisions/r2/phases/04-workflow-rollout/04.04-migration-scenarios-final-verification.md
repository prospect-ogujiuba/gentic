# Subphase 04.04: Migration, scenarios, hardening, and final verification

Created: 2026-08-30
Purpose: Prove the full canonical workflow, compatibility boundary, and release quality.
Status: Pending — blocked by 04.03
Topic: `pi-swe/canonical-planning`
Spec: `.model-artifacts/specs/pi-swe/canonical-planning/2026-08-30_0331-initiative-spec-r2.md`
Plan: `.model-artifacts/plans/pi-swe/canonical-planning/2026-08-31_2110-plan-index-r2.md`
Plan revision: 2

## Goal and observable behavior

Documentation and tests guide users from legacy or new planning into the canonical lifecycle; automated checks and representative manual scenarios pass, and final review finds no unresolved high/critical issue.

## Dependencies

- `04.03`

## Expected files

- Update `extensions/pi-swe/README.md`.
- Rewrite/extend `extensions/pi-swe/docs/e2e-scenarios.md`.
- Update `test/pi-swe.test.ts`, `test/pi-swe-lifecycle.test.ts`, runtime tests, and repository checks as warranted.
- Add migration/adoption helper code only if earlier resolver contracts require it; keep it extension-local and guidance-first.
- Update inventory/catalog only if generated checks require it; no new command expected.

## Implementation contract

1. Document canonical hierarchy, lifecycle, gates, trivial profile, command syntax, standalone operation, optional todo derivation, and explicit legacy adoption.
2. Add manual scenarios: canonical feature; bug→diagnose→TDD; DSA; plan review/revision; stale approval; legacy adoption; two initiatives/ambiguity; no pi-todo; with pi-todo; blocked handoff; fresh-session resume; approved deferral; atomic contract completion/recovery; stable filename status projection; finalize gate.
3. Keep existing migration references for Programming SOP, TDD RGR, and DSA Advisor plus intentional omissions.
4. Add automated fixtures for malformed/future manifests, path traversal, cycles, stale revision, ambiguous candidates, and no-verifier.
5. Run focused tests after each fix; then test:swe, typecheck, check, full tests.
6. Perform implementation review against this spec and plan. Fix in scope or record residual risk and explicit follow-up.
7. Produce durable verification and final handoff reports linked to exact plan revision.

## TDD boundary

Red first: documentation/resource assertions for every new scenario and canonical path contract; implementation tests should already be Red/Green from prior phases. Test level: integration/resource/end-to-end.

## Acceptance criteria

- `npm run test:swe`, `npm run typecheck`, `npm run check`, and `npm test` pass.
- Manual sample covers at least new-plan approval, stale plan rejection, legacy adoption guidance, and resume.
- No canonical write instruction points to `.model-artifacts/todo/<topic>/phases/`.
- Canonical and legacy modes cannot be mixed silently.
- Atomic completion, recovery, idempotency, stable filenames, and human-readable status projection are covered before final verification.
- Final review decision is approve with no high/critical unresolved finding.

## Verification evidence

Record commands, exit codes, scope, gaps, and manual outcomes in `.model-artifacts/reports/pi-swe/canonical-planning/<timestamp>-verification.md`. Final handoff summarizes changed files, behavior, migration, evidence, deferrals, and residual risks.
