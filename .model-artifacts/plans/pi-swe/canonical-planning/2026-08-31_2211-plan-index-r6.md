# Canonical pi-swe planning — execution plan r6

Created: 2026-08-31
Purpose: Make stage artifact-presence invariants consistent with consuming POSIX renames and resumable rollback.
Status: Approved — ready for execution
Initiative: `pi-swe/canonical-planning`
Spec: `.model-artifacts/specs/pi-swe/canonical-planning/2026-08-30_0331-initiative-spec-r2.md`
Predecessor: `.model-artifacts/plans/pi-swe/canonical-planning/2026-08-31_2158-plan-index-r5.md`
Contract root: `.model-artifacts/plans/pi-swe/canonical-planning/revisions/r6`
Approval: `.model-artifacts/reports/pi-swe/canonical-planning/2026-08-31_2214-plan-r6-review.md`
Blocking findings: 0
First ready contract: `04.03`

## Revision rationale

R5 closed journal stages but incorrectly required prepared source files to verify after POSIX rename had consumed them. Plan r6 makes target hash and prepared-path presence conditional per role and preserves rollback backups by restoring through separate temporary files.

R5 peer review: `.model-artifacts/reports/pi-swe/peer-review/2026-08-31_2210-plan-r5-rereview.md`.

## Execution policy

This exact revision is approved and executable; manifest approval matches this plan revision/path/hash and r6 review. Implement one exact ready subphase at a time. Manifest and `contracts.json` machine state are authoritative; active canonical filenames remain stable. Completion requires exact verification and approving implementation review followed by the explicit recoverable completion action. Orchestration remains guidance-only otherwise.

## Dependency graph

```text
01.01 → 01.02 → 01.03
01.03 → 02.01 → 02.02 → 02.03
02.03 → 03.01 → 03.02 → 03.03
03.03 → 04.01 → 04.02 → 04.03 → 04.04
```

Phases 01–03 and contracts 04.01–04.02 are complete. `04.03` alone is ready. `04.04` remains dependency-blocked.

## Specialist incorporation

- DSA/readiness: existing dependency DAG and lowest-ready selection remain.
- TDD: covers each target pre/new × prepared present/absent stage combination, rename-before-stage gaps, restore-temp gaps, rollback, validated cleanup, reload identity, and state cross-invariants.
- Migration/compatibility: optional completion records and stable paths remain backward-compatible; only the approved historical marker exception remains.
- Operations: closed stages, consumed-source rules, non-consuming backup restoration, fsync, cleanup, and blocked recovery are explicit.
- Security/performance/accessibility/diagnosis: no new boundary or scale concern; bounded path-confined schemas and existing text output apply.

Evidence: `.model-artifacts/findings/pi-swe/canonical-planning/2026-08-31_2112-completion-transition-specialist-review.md` plus exact r6 incorporation.

## Verification matrix

| Scope | Evidence |
|---|---|
| Existing 01.*–04.02 | existing lifecycle/resource suites and reviews |
| 04.03 schema/state | completion-record complete disposition and same-snapshot nextState tests |
| 04.03 transaction | every stage, rename-before-update, consumed prepared paths, restore-temp interruption, rollback, validated cleanup subsets, corruption/blocked recovery |
| Boundary | canonical inspector, `npm run test:swe`, `npm run typecheck`, `npm run check` |
| 04.04 | full regression/manual scenarios/final review after 04.03 |

## Migration and rollback

Historical r1 keeps its approved marker; r6 paths stay stable. Before durable `validated`, rollback reads preserved backup bytes into separate fsynced restore temporaries and atomically installs those temporaries, leaving backups available across interrupted restores. At/after `validated`, new state is committed and recovery only cleans remaining artifacts. Impossible hash/presence combinations block recovery.

## Approval and readiness

Decision: approved for exact revision r6. Approval records match exact plan/review/hash. Blocking findings: 0. First ready contract: `04.03`. Expected 04.04/finalize blockers do not block execution.

## Open blockers

None for starting 04.03.