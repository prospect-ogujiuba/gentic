# Pi primitives full review

## Correctness and security fixes

- Reject non-object configuration and non-boolean `enabled` values with visible `config` diagnostics.
- Report every explicit registry entry as disabled when global enablement is false.
- Canonicalize primitive resource roots lazily and reject lexical or symlink escapes while preserving valid dot-prefixed filenames.
- Reject blank phrase and regex trigger entries so conditional policies cannot become accidentally always-on.
- Bound recursive scans by characters, visited values, enumerated property names, and depth; preserve fail-closed behavior for cycles and accessors.
- Detect duplicate policy headings as exact lines, including CRLF resources, instead of broad substring matches.

## Cleanup and consistency

- Consolidated duplicated prompt-event extraction in `matchesPrimitivePrompt`.
- Renamed load-report terminology to registration-report terminology in code, status, tests, and documentation.
- Preserved the intentional no-op `whimsical` compatibility registry entry and native Pi working indicator.
- Updated README configuration, containment, trigger, and prompt-idempotence contracts.
- Corrected one stale repository-wide `/pi-hud mode` completion description/assertion discovered by the full suite; the removed `footer` mode is no longer advertised.

## Regression coverage

Focused coverage now includes malformed config, global disable status, isolated sync/async initializer failures, invalid JSON/regex/blank triggers, symlink traversal, valid dot-prefixed resources, exact character/node/depth/property bounds, cycles, shared references, own/inherited accessors, CRLF and exact-line idempotence, trigger-content non-disclosure, and multi-policy interoperability.

## Final sweep

No TODO/FIXME/HACK/XXX markers, discovery scans, dynamic imports, cache-busting imports, resource symlinks, registry/resource drift, blank triggers, invalid bundled regexes, oversized policies, or diff whitespace errors remain in scope. Full test, typecheck, package, command-inventory, and performance verification are required after this report write and are recorded in `workflow.json`.