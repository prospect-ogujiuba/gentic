# p01-c02-tdd-cycle

Created: 2026-09-11T21:39:19.306Z
Purpose: Record runtime Red, Green, and Refactor evidence for P01-C02 runner persistence behaviors.

# P01-C02 TDD cycle

## Atomic runner lease round trip

- Behavior: Persist and recover one bounded versioned runner lease for a topic.
- Test level: integration.
- Red evidence: `node --experimental-strip-types --test test/pi-swe-runtime.test.ts` failed with `ERR_MODULE_NOT_FOUND` for `runner-persistence.ts`.
- Green evidence: Added atomic temp-write/fsync/rename persistence and strict topic-bound read; focused test passed.
- Refactor evidence: none; focused test remained green.
- Verification: focused runtime test.
- Follow-up: none.

## Fail-closed validation and ownership

- Behavior: Reject conflicting owners and unsafe, malformed, partial, oversized, mismatched-topic, symlinked, and future-version state.
- Test level: integration.
- Red evidence: focused runtime test accepted a conflicting owner; after extending malformed coverage it also accepted a partial JSON runner.
- Green evidence: Added existing-owner validation, bounded reads, future-version rejection, symlink/path checks, and strict runner-envelope validation; focused test passed.
- Refactor evidence: Tightened topic segment and monotonic sequence invariants; focused runtime and runner-domain tests passed.
- Verification: focused runtime and runner-domain tests.
- Follow-up: none.

## Crash-window recovery and operator controls

- Behavior: Prepared dispatch recovery stops as uncertain, sent dispatch recovery awaits its checkpoint, accepted checkpoints resume evaluation, and pause/stop plus sent acknowledgement are idempotent.
- Test level: integration.
- Red evidence: focused runtime test failed because `markPiSweRunnerDispatchSent` and persisted pause/stop APIs did not exist.
- Green evidence: Added prepared/sent delivery state, recovery classification, token-bound sent acknowledgement, and owner-bound pause/stop persistence; focused test passed.
- Refactor evidence: Prevented pause from reviving inactive runs and preserved completed runs on stop; focused runtime and runner-domain tests passed.
- Verification: `npm run typecheck` passed; `npm run test:swe` passed (189 passed, 1 skipped, 0 failed).
- Follow-up: Controller wiring and message dispatch remain in later approved contracts.
