# tdd-cycle

Created: 2026-09-11T21:20:26.990Z
Purpose: Record runtime Red, Green, Refactor, and verification evidence for P01-C01.

# TDD cycle: P01-C01 runner domain

## Behavior: deterministic runner decisions and validated checkpoints

- Test level: unit.
- Acceptance: AC-02, AC-03, AC-04, AC-06, AC-09.
- Red evidence: `node --experimental-strip-types --test test/pi-swe-runner-domain.test.ts` exited 1 because `createPiSweRunner` was not exported before production changes.
- Green evidence: added the pure normalized runner record/reducer in `extensions/pi-swe/src/domain/runner.ts`, exported it from `extensions/pi-swe/src/lifecycle.ts`, and the focused command passed 6/6 tests.
- Refactor evidence: none; the post-green review found the focused module already isolated and transition/retry authority reused from lifecycle code. Focused tests remained green.
- Verification: focused runner-domain tests passed 6/6; `npm run typecheck` passed; `npm run test:swe` passed 186 tests with 1 skipped and 0 failures.
- Follow-up: persistence, command/event wiring, and completion mutation remain explicitly outside P01-C01.

## Behavior: contradictory duplicate checkpoints fail closed

- Test level: unit.
- Acceptance: AC-04 and AC-09.
- Red evidence: the focused test exited 1 because an altered outcome using an accepted dispatch token was incorrectly treated as an idempotent duplicate.
- Green evidence: the runner now retains a deterministic accepted-checkpoint key; only an exact duplicate is a no-op, while a contradictory reuse blocks.
- Refactor evidence: checkpoint-key construction was isolated in one pure helper; focused tests passed.
- Verification: focused tests passed 6/6; `npm run typecheck` passed; `npm run test:swe` passed 186 tests with 1 skipped and 0 failures.
- Follow-up: none for this behavior.

## Behavior: retry budgets are isolated by failure identity

- Test level: unit.
- Acceptance: AC-06 and AC-09.
- Red evidence: with `maxRetries=1`, a distinct verify failure signature incorrectly inherited the first signature's retry and the focused test exited 1.
- Green evidence: the normalized record now retains bounded retry counts keyed through the existing canonical retry identity; distinct signatures receive separate budgets and a repeated signature stops.
- Refactor evidence: retry-key derivation remains local and delegates stop evaluation to `evaluateAutonomousRunnerStep`; focused tests passed.
- Verification: focused tests passed 6/6; `npm run typecheck` passed; `npm run test:swe` passed 186 tests with 1 skipped and 0 failures.
- Follow-up: persistence bounds for the retry-count record belong to P01-C02.
