# tdd-cycle

Created: 2026-09-11T23:57:49.467Z
Purpose: Record runtime Red, Green, Refactor, and verification evidence for P02-C01 command and checkpoint behaviors.

# P02-C01 TDD cycle

## Behavior 1: conservative `/swe work` authorization

- Behavior: status performs no write; explicit idle start creates one exact canonical runner with parsed mode, target, and bounded budgets; invalid or busy starts do not create state.
- Test level: integration command-handler test with fake Pi context and temporary canonical repository.
- Red evidence: `npm exec -- node --experimental-strip-types --test test/pi-swe-work.test.ts` failed 2/2 before production changes because `/swe work` was unrecognized and no runner state was created.
- Green evidence: added conservative work parsing, start/status/pause/resume/stop handlers, canonical identity binding, persistence, and bounded status output; focused command tests passed.
- Refactor evidence: tightened numeric option parsing to plain positive decimal integers after a new Red test showed scientific notation (`1e2`) was accepted; focused tests returned green.
- Verification: focused file passed; `npm run typecheck` passed; `npm run test:swe` passed with 193 passing, 0 failing.
- Follow-up: settled-turn dispatch remains explicitly outside P02-C01 and belongs to P02-C02.

## Behavior 2: structured stage checkpoint

- Behavior: only the active dispatch token with exact canonical run/plan/contract/stage identity and hash-valid topic evidence is accepted; stale, escaped, or contradictory calls produce no continuation.
- Test level: integration tool test with persisted runner state and temporary canonical repository.
- Red evidence: the focused test failed with `structured checkpoint tool must be registered` before production changes.
- Green evidence: registered `swe_checkpoint`, added bounded TypeBox schema, canonical reinspection, evidence path/hash validation, pure reducer validation, and atomic persistence; focused tests passed.
- Refactor evidence: extracted identity, evidence, and result helpers after green; focused tests remained green.
- Verification: focused file passed; `npm run typecheck` passed; `npm run test:swe` passed with 193 passing, 0 failing.
- Follow-up: checkpoint-triggered dispatch remains explicitly outside P02-C01 and belongs to P02-C02.
