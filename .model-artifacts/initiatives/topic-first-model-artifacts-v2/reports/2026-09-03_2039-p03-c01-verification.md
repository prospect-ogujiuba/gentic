# Verification evidence: P03-C01 migration planner

Timestamp: 2026-09-03 20:39 EDT
Scope: `P03-C01`, plan revision 3, `.model-artifacts/initiatives/topic-first-model-artifacts-v2/plans/revisions/r3/phases/03-migration-engine/03.01-inventory-plan.md`, `sha256:2f502a693e60853acf6ee485b0ac1dc99e100d7f24705fef81e0460173cfe02e`.

Canonical gate: active spec, approved plan r3, active contract pointer, contract index entry, predecessor completion records, and SHA-256 links match. No prior P03-C01 verification artifact was found.

## Acceptance-to-evidence map

- AC1: Deterministic sorted relocation groups all six initiative kinds beneath one v2 topic root and system records beneath `system`.
  - Check/evidence: focused tests `migration plans have stable logical fingerprints and sorted eligible moves`, `complete v1 topic authority plans one deterministic topic-first relocation and exact rewrites without candidate mutation`, and `artifact audit recognizes layout-v2 initiative/system namespaces and relocates legacy system records`.
  - Result: pass.
- AC2: Mixed authority, ambiguous ownership, unsupported files, collisions, stale/unsafe references, symlinks, and bounds are blockers.
  - Check/evidence: focused mixed-authority, inbound-reference, invalid/symlink classification, stale authority hash, active claim, duplicate/case-fold collision, and bound tests; source inspection of `inventory.ts` and `plan.ts`.
  - Result: partial. Existing tests do not prove planner-level symlink refusal after inventory, stale inventory/reference revalidation between audit and plan, or all declarative invalid normalization fixtures.
- AC3: Protected canonical v1 files become migratable only as a complete authority set.
  - Check/evidence: complete-v1-authority fixture plus historical-contract protection test; incomplete/cross-topic authority paths remain diagnostic/protected in `validateV1Authority`.
  - Result: pass.
- AC4: Plan records source fingerprints, reference sites, destinations, expected hashes, bounds, and rollback estimate.
  - Check/evidence: complete-authority fixture assertions and source inspection of `MigrationPlan`, `buildTransforms`, report rendering, and command output.
  - Result: partial. A distinct staging-byte limit/measurement required by the incorporated performance decision is absent; nested rewrite records and bounds are not closed-schema validated when a saved plan is loaded.
- AC5: Identical bytes produce an identical semantic fingerprint.
  - Check/evidence: two generated-at values over identical inventory produce the same fingerprint in focused tests.
  - Result: pass.

## Planned verification map

- Classification fixture tables: focused pi-artifacts suite — pass.
- Normalization fixture tables: path/config coverage exists, but `layoutV2.invalid` is not exercised by pi-artifacts — gap.
- Ordering: sorted move and stable fingerprint tests — pass.
- Collision/conflict: mixed, duplicate, occupied, and case-fold coverage — pass.
- Reference closure: complete authority and inbound reference coverage — partial; no overlapping-prefix or dependency-cycle test and no audit-to-plan stale-source scenario.
- Bounds: inventory, affected, references, rewrite, and rollback coverage — partial; staging bound is absent.
- Schema transforms: manifest rewrite is asserted only by presence — partial; transformed schema value, dependent content hashes, malformed nested plan records, and cycle rejection are not asserted.
- Stable fingerprinting: focused test — pass.
- Command integration dry-run: `/artifacts` audit/plan command integration and system-log persistence tests — pass.

## Checks

- Command: `npm run typecheck`
  - Result: pass, exit 0.
  - Evidence summary: TypeScript `tsc --noEmit` completed without errors.
- Command: `node --experimental-strip-types --test test/pi-artifacts.test.ts`
  - Result: pass, exit 0.
  - Evidence summary: 23 tests passed; 0 failed.
- Command: `npm test`
  - Result: pass, exit 0.
  - Evidence summary: 410 tests passed; 0 failed; 0 skipped.
- Command: `npm run check`
  - Result: pass, exit 0.
  - Evidence summary: Pi API, catalog, inventory, anatomy, and package resource checks passed.
- Command: `git diff --check`
  - Result: pass, exit 0.
  - Evidence summary: no whitespace errors.
- Manual/source coverage inventory
  - Result: partial.
  - Evidence summary: no `layoutV2.invalid`, `reference-cycle`, overlapping-path-prefix, staging-bound, `parseRewrite`, or `parseBounds` verification was found. The apparent `prefix` match was command-completion code, not reference-trie coverage.

## Gaps

- Add explicit `maxStagingBytes`/`stagingBytes` planning bounds and report evidence.
- Revalidate inventory source/reference fingerprints at plan creation so audit-to-plan byte changes block.
- Closed-schema validate every rewrite, bounds, authority-unit, and expected-hash record loaded from saved plans.
- Add table/property tests for invalid normalization fixtures, symlink planner blockers, overlapping path references, dependency-cycle rejection, exact manifest schema-v2 transformation, and dependent post-transform hashes.

## Outcome

Partial. Compilation and all current tests/checks pass, but the complete planned verification map has gaps and AC2/AC4 are only partial. P03-C01 completion is blocked. Return to `/skill:swe-implement` for the listed narrow follow-up within the existing contract; no plan revision is required unless the implementation cannot add the staging bound or closed-schema validation without changing the approved contract.
