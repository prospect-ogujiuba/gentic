# Contract P01-C01: Fixture and record schemas

- Topic: `pi-swe-model-evaluation`
- Plan revision: 2
- Kind: executable subphase
- Parent: `P01`
- Status: pending
- Depends on: none

## Goal and observable behavior

Add an `evals/pi-swe-autonomy/` package boundary that can materialize a deterministic synthetic repository containing a tiny testable Node behavior plus a valid approved three-contract Pi-SWE plan. The materializer emits a fixture digest and validates all record schemas.

## Scope

- `evals/pi-swe-autonomy/src/types.ts`
- `evals/pi-swe-autonomy/src/fixture.ts`
- `evals/pi-swe-autonomy/fixtures/approved-plan/**`
- focused tests in `test/pi-swe-model-eval.test.ts`

## Non-goals

No live model call, scorer, CLI, existing Pi-SWE schema change, or Marathon integration.

## Entry inputs

Repository Pi-SWE schema-v2 reader behavior and Node.js 22 filesystem/crypto APIs.

## Specialist decisions incorporated

Incorporates `.model-artifacts/initiatives/pi-swe-model-evaluation/findings/2026-09-11_1214-dsa-decision.md`, `2026-09-11_1214-tdd-plan.md`, and `2026-09-11_1214-security-operations-compatibility.md`. Use versioned plain objects, append-only event JSONL, stable lexical ordering, and SHA-256 fixture/tree identities. Do not add SQLite until measured run volume or resumability requires it. Fixtures contain no credentials; cleanup resolves realpaths and cannot traverse symlinks outside the evaluator run root.

## Acceptance criteria

- AC-01a: Two materializations have identical content-tree digests and no shared writable files.
- AC-01b: The fixture has three dependency-ordered executable contracts and valid approval/hash links.
- AC-01c: Trial/event/score records reject unknown schema versions and missing identity fields.
- AC-01d: Cleanup cannot escape the evaluator-owned temporary root.

## Verification

Red-first unit tests for deterministic materialization, schema rejection, canonical inspection, and cleanup path confinement; then `npm run typecheck` and focused Node tests.

## Rollback

Remove the new evaluator directory and focused tests; no persistent data migration.
