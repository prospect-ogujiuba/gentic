# p02-c01-verification

Created: 2026-09-13T17:40:45.189Z
Purpose: Record guided verification evidence and fix-in-contract disposition for canonical contract P02-C01.

# Verification evidence: P02-C01 surfaces and qualification

Timestamp: 2026-09-13 17:40 UTC
Scope: canonical contract `P02-C01`, plan revision 2, `.model-artifacts/initiatives/pi-context-pressure-guardrails/plans/revisions/r2/phases/02-operator-integration/02.01-surfaces-and-qualification.md` at `sha256:f1099cbc33f4237fa44cb1231b3b6f092fad6d05e3f745f024c63ca0d94432ef`.

## Acceptance-to-evidence map

- AC-07 — summary/artifact/JSON/HUD expose consistent additive pressure data without raw content.
  - Check/evidence: focused report/HUD tests plus an independent sentinel probe across `renderPiContextJson()` and `createPiContextHudSnapshot()`.
  - Result: `fail`. Additive pressure status is consistent and focused tests pass 9/9, and HUD excludes both sentinels. JSON retains the raw source path `/home/private/credential-source.txt` and raw label `raw secret prompt` through ledger groups.
- AC-08 — README and end-to-end documentation explain configuration, transitions, limitations, and operator response.
  - Check/evidence: direct README review and the resource-check portion of `npm run check`.
  - Result: `pass`. The README covers versioned global/project configuration, precedence, defaults, hysteresis/rearming, unavailable usage, operator response, advisory-only behavior, privacy, and a focused qualification scenario. Resource checks completed before the later model-artifact check failure.
- AC-09 — focused pi-context tests, typecheck, package checks, and full suite pass.
  - Check/evidence: commands listed below.
  - Result: `fail`. Focused tests, all pi-context tests, typecheck, and the full test suite pass. `npm run check` exits 1 because the runner-owned `.model-artifacts/system/logs/pi-swe/pi-context-pressure-guardrails/runner.json` is rejected as a non-v2 artifact by `check:model-artifacts`.
- JSON/HUD contain no raw prompt, tool result, argument, credential, or source path fields.
  - Check/evidence: sentinel probe with a raw prompt label and source path in a normalized maintained ledger entry.
  - Result: `fail` for JSON; `pass` for HUD.
- Existing command aliases and report sections remain valid.
  - Check/evidence: focused report tests, all pi-context tests, typecheck, and full suite.
  - Result: `pass`.
- Scope/non-goals.
  - Check/evidence: changed-path allowlist and implementation diff inspection.
  - Result: `pass`. Exactly the six approved P02-C01 paths changed; no pi-hud redesign, new command namespace, automatic response, or external action was added.

## Checks

- `node --experimental-strip-types --test test/pi-context-report.test.ts test/pi-context-hud-adapter.test.ts`
  - Result: pass, exit 0; 9 passed, 0 failed.
- Independent JSON/HUD privacy sentinel probe
  - Result: fail, exit 1; `privacy sentinel leak: json path, json prompt`. HUD contained neither sentinel.
- `node --experimental-strip-types --test test/pi-context*.test.ts`
  - Result: pass, exit 0; 34 passed, 0 failed.
- `npm run typecheck`
  - Result: pass, exit 0.
- `npm run check`
  - Result: fail, exit 1; all checks through resource validation completed, then `check:model-artifacts` rejected `.model-artifacts/system/logs/pi-swe/pi-context-pressure-guardrails/runner.json` as non-v2.
- `npm test`
  - Result: pass, exit 0; 504 tests observed, 503 passed, 0 failed.
- Contract changed-path allowlist
  - Result: pass; six expected paths, zero outside-contract paths.

## Gaps

- JSON privacy is not satisfied for maintained ledger entries that carry raw labels or source paths.
- The package check cannot pass while the active runner log is treated as an invalid model artifact.

## Outcome

`fix-in-contract`: verification does not pass. Return to `swe-implement` for one bounded correction in `extensions/pi-context/src/app/report.ts` and `test/pi-context-report.test.ts`: preserve the JSON structure/compatibility while redacting raw prompt/content labels and source-path values, then rerun the complete verification map. The runner-owned model-artifact check failure must also be reconciled or explicitly excluded before AC-09 can pass.
