# Verification evidence: SWE completion tool surface

Timestamp: 2026-09-05 01:25 UTC
Scope: Contract `P02-C01`, plan revision 3, `.model-artifacts/initiatives/swe-completion-tool/plans/revisions/r3/phases/02-tool-surface/02.01-tool-surface.md` (`sha256:c5cdd26bcf25c9e3b02cfb6c029cb020b31e40ab070fb2da5fa5cf29fd3960ee`). Changed scope: `extensions/pi-swe/src/pi/tools.ts`, `extensions/pi-swe/index.ts`, focused completion/extension tests, README/e2e documentation, and generated Gentic inventory.

Pi-SWE-Evidence: {"schemaVersion":1,"mode":"verification","topic":"swe-completion-tool","contractId":"P02-C01","contractPath":".model-artifacts/initiatives/swe-completion-tool/plans/revisions/r3/phases/02-tool-surface/02.01-tool-surface.md","planRevision":3,"contractContentHash":"sha256:c5cdd26bcf25c9e3b02cfb6c029cb020b31e40ab070fb2da5fa5cf29fd3960ee","outcome":"pass","gaps":"none"}

## Acceptance-to-evidence map

- Criterion 1 — exactly one `swe_complete` registration with optional `topic`/`contractId`/`next`, required boolean `confirm`, and Google-compatible `clear|advance` enum.
  - Check/evidence: focused schema test; source inspection of `extensions/pi-swe/src/pi/tools.ts`; generated `catalog/gentic-inventory.json` contains exactly `swe_complete` for pi-swe.
  - Result: pass.
- Criterion 2 — `confirm !== true` rejects before resolver/evidence I/O or writes.
  - Check/evidence: `swe_complete refuses before resolver I/O unless confirm is true` uses throwing resolver/transaction spies and observes zero calls; source order places the exact confirmation check before resolver invocation.
  - Result: pass.
- Criterion 3 — `next` defaults to `advance` and preserves `clear`.
  - Check/evidence: direct tool execute test observes `advance` then `clear` at the resolver boundary; completion regression covers clear/no-advance semantics.
  - Result: pass.
- Criterion 4 — resolver and transaction dispositions remain distinguishable with bounded text/details and no report bodies.
  - Check/evidence: direct execute tests cover resolver `rejected`, transaction `completed` and `conflict`; source inspection confirms generic pass-through for `rejected|conflict|blocked-recovery|completed|already-complete`, 2048-character text, 512-character message/artifact, and 8-item detail bounds. Resolver and transaction return metadata only, never report bodies.
  - Result: pass.
- Criterion 5 — valid execute delegates exactly once and existing claim remains concurrency authority.
  - Check/evidence: spy test counts one transaction call per resolved execute; real tool integration completes a valid fixture with no residual journal; full completion regression passes exclusive-owner and separate-process one-winner tests. No outer mutation queue was added.
  - Result: pass.
- Criterion 6 — low-level command/API, normalization, records, recovery, and faults remain compatible.
  - Check/evidence: production diff does not modify `completion.ts` or command parsing; full 36-test completion regression passes command behavior, replay, schema-v1 normalization, completion records, claim ownership, recovery, and every durable fault boundary.
  - Result: pass.
- Criterion 7 — README/e2e documentation covers concise input, inference, fail-closed evidence, retained command, and non-autonomous behavior.
  - Check/evidence: manual source review of `extensions/pi-swe/README.md` and `extensions/pi-swe/docs/e2e-scenarios.md`; pi-swe documentation assertions pass.
  - Result: pass.
- Criterion 8 — required focused and broad commands pass.
  - Check/evidence: commands and results below.
  - Result: pass.

## Planned verification map

- Tool registration/schema and test-double compatibility: focused tool tests and `test/pi-swe.test.ts` within `npm run test:swe`; pass.
- Confirmation-before-I/O spy/seam: focused execute test; pass.
- Explicit/inferred complete/advance, `clear`, and replay: real explicit tool integration, inferred resolver integration, adapter boundary tests, and completion transaction clear/replay regressions; pass.
- Missing/ambiguous/stale evidence mapping: resolver rejection adapter test plus full resolver evidence tables; pass.
- Concurrent calls and completion fault suite: exclusive claim, separate-process one-winner, recovery, and fault-boundary tests in the 36-test completion regression; pass.
- README/e2e assertions: pi-swe suite plus manual content inspection; pass.
- Contract exit commands: `npm run test:swe`, `npm run typecheck`, `npm run check`, and `npm test`; pass.
- TDD execution evidence: registration Red observed zero tools before production wiring; compatibility Red observed five missing-`registerTool` test-double failures; focused and broad Green/Refactor reruns pass; pass.

## Checks

- `node --experimental-strip-types --test --test-name-pattern='swe_complete|/swe complete|exclusive local completion owner|completion fault|recovery|legacy index' test/pi-swe-completion.test.ts`
  - Result: pass, exit 0; 10/10 selected tests passed.
  - Evidence summary: schema, confirmation, default/clear, bounded rejection, real guarded completion, ownership, recovery, low-level command, and migration behaviors passed.
- `node --experimental-strip-types --test test/pi-swe-completion.test.ts`
  - Result: pass, exit 0; 36/36 tests passed.
  - Evidence summary: resolver safety, replay, contention, fault injection, recovery, schema normalization, records, and command compatibility passed.
- `npm run test:swe`
  - Result: pass, exit 0; 159/159 tests passed.
- `npm run typecheck`
  - Result: pass, exit 0; `tsc --noEmit` reported no errors.
- `npm run check`
  - Result: pass, exit 0; Pi API, catalog, generated inventory, anatomy, resources, and model-artifact checks passed.
- `npm test`
  - Result: pass, exit 0; 433/433 tests passed.
- Contract/spec/plan hash validation and `git diff --check`
  - Result: pass; exact manifest links match and all seven implementation files are within contract scope.

## Gaps

None known for `P02-C01`.

## Outcome

Pass. Every acceptance criterion and planned verification item has current evidence with no gaps. `P02-C01` is eligible for implementation review; canonical completion remains blocked until a fresh approving implementation-review artifact binds this verification report.
