# Actual primitive scaffold apply workflow

In an isolated Pi project with isolated HOME, loaded current `pi-commands` and a copied current `pi-primitives`, then executed the real TUI command:

`/scaffold primitive live-check --apply`

The TUI reported creation of:

- `extensions/pi-primitives/primitives/live-check/index.ts`
- `extensions/pi-primitives/primitives/live-check/injection.md`
- `extensions/pi-primitives/primitives/live-check/triggers.json`

It also displayed the required next step to import `live-check` and add it to `EXPLICIT_PRIMITIVES`. After performing that explicit temporary registry edit:

- isolated TypeScript compilation passed;
- a Node test loaded the generated primitive through the copied host;
- exactly four prompt hooks were registered (three retained policies plus the generated primitive);
- an unrelated prompt did not receive `# live-check`;
- a matching prompt received `# live-check` without copying `SECRET_SENTINEL` trigger content;
- session status was exactly `5 registered`;
- live `/reload` succeeded and the TUI remained at `5 registered` with no warning.

The Pi process was terminated and all temporary project, HOME, generated files, registry edits, test harness, and symlinked dependencies were removed.