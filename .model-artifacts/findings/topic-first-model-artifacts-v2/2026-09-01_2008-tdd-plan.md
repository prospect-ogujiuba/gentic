# TDD Planning Finding: Layout v2 Migration

- Topic: `topic-first-model-artifacts-v2`
- Reviewed spec: `.model-artifacts/specs/topic-first-model-artifacts-v2/2026-09-01_2008-initiative-spec-r1.md`
- Reviewed plan: `.model-artifacts/plans/topic-first-model-artifacts-v2/2026-09-01_2008-plan-index-r1.md`
- Decision: required; incorporate Red ordering into plan revision

## Next observable behaviors and test levels

1. **Layout classification (unit):** given canonical v2 initiative/system, v1, mixed, unsafe, and invalid examples, each local adapter returns the same classification.
2. **Todo generation (unit/integration):** creating each initiative kind writes exactly `.model-artifacts/initiatives/<topic>/<kind>/<timestamp>-<name>.md`; v1-only and mixed topics return migration/conflict repairs.
3. **pi-swe authority (unit/integration):** schema-v2 v2 authority validates; v1-only inspects read-only; dual authority blocks planning/execution; hashes and contract roots remain exact.
4. **Migration planning (unit/integration):** a complete v1 fixture yields one deterministic zero-mutation plan; ambiguous, colliding, stale, unsupported, symlinked, mixed, or over-bound fixtures block.
5. **Structured transform (unit):** exact path fields/references and dependent hashes transform in topological order; cycles or unenumerated references block.
6. **Transaction safety (integration):** apply under each injected interruption recovers idempotently; rollback restores byte-identical v1; modified v2 bytes block rollback; finalize is explicit.
7. **Atomic integration (repository):** all extension suites and release checks pass against v2 while explicit v1 fixtures remain compatibility-only.
8. **Repository rollout (end-to-end/operator):** Gentic audit→plan→review→apply→audit is reproducible; DevArch stays gated until Gentic release verification.

## Characterization needs

Before changing production code, preserve current tests for:

- pi-todo filename/body/evidence rules;
- pi-swe schema-v1 hash, approval, readiness, completion, and contract graph behavior;
- pi-artifacts deterministic inventory, reference protection, collision refusal, claims, apply, and rollback;
- pi-context lifecycle/report semantics and package resource injection.

Characterization must separate behavior intentionally retained from kind-first assertions intentionally replaced.

## Red ordering

1. Add declarative layout conformance fixtures and focused Red assertions without production edits (`P01-C01`).
2. Make pi-primitives/pi-todo generation and validation green (`P02-C01`).
3. Make pi-swe and other readers/writers green for v2/v1-read-only/mixed-blocked (`P02-C02`).
4. Add planner Red tables, then implement deterministic inventory/plan (`P03-C01`).
5. Add transaction failure-injection Reds one journal boundary at a time, then implement apply/recover/rollback/finalize (`P03-C02`).
6. Run repository integration and migration rehearsal before self-migration (`P04-C01`).
7. Define DevArch repository-specific verification before approving its saved plan (`P05-C01`).

No plan-time Red evidence is claimed. Each implementation contract must record actual command and observed expected failure before production changes.

## Verification scope

- Focused commands: `npm run test:primitives`, `npm run test:todo`, `npm run test:swe`, direct `test/pi-artifacts.test.ts`, affected pi-context/runtime tests.
- Nearby checks: package resources, catalog/inventory/anatomy/command/performance checks.
- Broad checks: `npm test` and `npm run release:verify` before and after Gentic self-migration.
- Migration evidence: tree fingerprints, audit summaries, saved plan/ledger/rollback bundle, and post-apply reference scan.

## Required plan revisions

- Make `P01-C01` explicitly tests-only and first-ready.
- Require actual Red evidence per subsequent behavior, never a single broad failing suite.
- Add topological structured-transform tests, every-boundary interruption tests, and exact byte rollback.
- Require repository-specific DevArch commands before its plan approval.
