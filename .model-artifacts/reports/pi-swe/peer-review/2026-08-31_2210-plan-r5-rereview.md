# plan-r5-rereview

Created: 2026-08-31T22:10:22.057Z
Purpose: Re-review exact plan r5 and commit eed8772 after r4 journal and completion-record findings.

# Plan r5 peer re-review

Mode: plan review
Decision: request changes
Commit: `eed8772`
Topic: `pi-swe/canonical-planning`
Plan: `.model-artifacts/plans/pi-swe/canonical-planning/2026-08-31_2158-plan-index-r5.md`
Contract: `.model-artifacts/plans/pi-swe/canonical-planning/revisions/r5/phases/04-workflow-rollout/04.03-atomic-completion-transition.md`

## Verification

- R4 completion-record disposition/next-state finding: resolved.
- R4 closed-stage/validated-cleanup structure: substantially incorporated.
- Active spec/plan and all 13 r5 contract paths/hashes: valid.
- Sole ready contract: `04.03`.
- `npm run test:swe`: pass, 117/117.
- `npm run check:resources`: pass.
- `npm run typecheck`: pass.
- `git diff --check HEAD~1..HEAD`: pass.

## Finding

### Medium — rename-gap stages require prepared files that rename has consumed

Affected: `.model-artifacts/plans/pi-swe/canonical-planning/revisions/r5/phases/04-workflow-rollout/04.03-atomic-completion-transition.md`, installation-stage bullets under implementation step 5.

The `prepared` stage allows the contract-index target to have its prepared/new hash for a crash after rename but before the next stage update, while also requiring both prepared files to verify. A POSIX rename consumes the contract-index prepared path, so this allowed target state cannot satisfy the artifact invariant. Likewise, `contract-index-installed` allows the manifest target to have its prepared/new hash after its rename gap while still requiring the manifest prepared file to verify.

Action: make prepared-file existence conditional per role and target hash. During installation stages, `target=preimage` requires its prepared path to exist and match the new hash; `target=new` requires the consumed prepared path to be absent. Both backups remain required before commit. `manifest-installed` has both targets new and both prepared paths absent. Define rollback handling for any remaining prepared paths, and validated/cleanup tolerance for consumed/removed paths. Add fixtures for every rename-source-consumed combination.

## Decision

Do not execute 04.03 from r5. The peer confirmed this contradiction and will publish immutable r6 with per-role target/prepared invariants. Re-review exact r6, synchronize canonical hashes, and rerun inspector and existing verification.
