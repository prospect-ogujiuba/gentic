# 04-03-tdd-cycle

Created: 2026-08-31T22:51:41.294Z
Purpose: Record Red/Green/Refactor evidence for approved contract 04.03.

# TDD cycle: canonical completion 04.03

Scope: exact approved plan r6 contract `04.03`, hash `sha256:96d732e2d03ea75812685dc6f9c04916789332697214b42a8f7faf5a6ed2ba27`.

## Behavior 1: durable completion record and explicit successful transition

- Test level: integration.
- Red: `node --experimental-strip-types --test test/pi-swe-completion.test.ts` exited 1 with `ERR_MODULE_NOT_FOUND` for planned `extensions/pi-swe/src/completion.ts`; 0 pass, 1 fail.
- Green: added completion service, completion-record parser/projection, closed exact-contract evidence envelopes, stable path transition, reload idempotency, and command action. Unrelated plan-review/pass artifacts fail the gate.
- Refactor: centralized deterministic completion request identity in the canonical planning module and reused it from service/parser; focused tests stayed green.
- Verification: success, clear/advance-safe repeat identity, mismatch conflict, stale/missing/wrong-active rejection, parser status cross-invariants, and historical advancement.

## Behavior 2: closed staged recovery

- Test level: integration with deterministic fault injection.
- Red: stage tests were authored before transaction production code and failed at the missing completion module boundary.
- Green: implemented prepared, contract-index-installed, manifest-installed, rollback, validated, and cleanup stages with fsync, consumed prepared-source invariants, retained backups, restore temporaries, reader blocking, and blocked recovery.
- Refactor: extracted stage invariant validation, committed-state validation, strict fsync helpers, owner-token-bound exclusive local claims/journal updates, no-auto-reap recovery handoff, orphan reconciliation, rollback restoration, and committed cleanup. Focused tests stayed green.
- Verification: faults before/after every durable install/commit stage, rename-before-stage gaps, rollback interruption, corrupted backup, impossible target/source combination, pre-journal write interruption/retry, same-snapshot mismatch rollback, partial cleanup roll-forward, incomplete/aged/dead claim ownership, claim-token replacement, and a true child-process loser rejected without touching the winner.

## Behavior 3: canonical versus legacy filename guidance and human status

- Test level: resource/command integration.
- Red: existing primitive asserted `[COMPLETE]` guidance without a canonical exemption and `/swe` completion was absent.
- Green: canonical contracts now use stable `contracts.json` state, legacy/non-canonical markers remain temporary, `/swe status` shows progress, and `/swe complete` reports disposition/next state.
- Refactor: kept orchestration guidance-only and completion as one separate explicit action.
- Verification: `test/pi-primitives.test.ts`, `/swe` command integration, documentation assertions, and stable filename checks pass.

## Follow-up

None within 04.03 before verification and implementation review.
