{
  "mode": "simple",
  "publicEntry": "index.ts",
  "layers": ["pi", "resources"],
  "resources": ["prompts"],
  "tests": ["npm run check"],
  "notes": [
    "Simple prompt hub: index.ts is a no-runtime owner namespace while prompts/ contains file-discovered prompt template resources."
  ]
}
