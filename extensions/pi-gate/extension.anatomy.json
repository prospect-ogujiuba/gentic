{
  "mode": "layered",
  "state": "layered-lite",
  "publicEntry": "index.ts",
  "layers": ["config", "domain", "app", "pi", "ui"],
  "resources": ["schemas"],
  "tests": ["npm run test:gate"],
  "notes": [
    "Layered-lite state: index.ts delegates to src/pi/register.ts and re-exports public test seams.",
    "Layer folders are intentionally lightweight: config holds config loading, domain holds policy decisions, app holds audit and remember persistence, pi wires events/commands, and ui renders prompts."
  ]
}
