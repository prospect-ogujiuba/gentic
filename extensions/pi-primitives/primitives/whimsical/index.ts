// Compatibility entry for existing primitive configuration and registration reports.
// Pi owns the native working indicator; do not install competing turn hooks.
export default function whimsical(): void {}
