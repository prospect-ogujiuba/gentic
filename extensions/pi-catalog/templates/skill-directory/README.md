# directory skill template

These inert template files represent a skill directory with optional helper and reference material. `SKILL.template.md` must become `SKILL.md` only in the scaffolded destination.
