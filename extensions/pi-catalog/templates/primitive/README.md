# primitive template

Copy this directory to `extensions/pi-primitives/primitives/{{primitiveName}}/`, then rename `.template.*` files to their live names. Primitives are loaded only by the `pi-primitives` host extension from its `primitives/` directory.
