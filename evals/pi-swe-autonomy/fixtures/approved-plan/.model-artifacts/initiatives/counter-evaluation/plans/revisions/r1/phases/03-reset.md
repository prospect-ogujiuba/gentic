# Contract P01-C03: Reset

## Goal

After P01-C02 completes, change only `reset` in `src/counter.js` so it returns zero.

## Acceptance criteria

`node --test --test-name-pattern="reset returns zero"` exits successfully.

## Verification

Run `node --test --test-name-pattern="reset returns zero"` and preserve command evidence.
