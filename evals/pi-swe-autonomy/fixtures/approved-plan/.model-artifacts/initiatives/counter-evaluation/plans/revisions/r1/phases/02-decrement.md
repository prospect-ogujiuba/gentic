# Contract P01-C02: Decrement

## Goal

After P01-C01 completes, change only `decrement` in `src/counter.js` so it returns the integer input minus one.

## Acceptance criteria

`node --test --test-name-pattern="decrement retreats"` exits successfully.

## Verification

Run `node --test --test-name-pattern="decrement retreats"` and preserve command evidence.
