# Contract P01-C01: Increment

## Goal

Change only `increment` in `src/counter.js` so it returns the integer input plus one.

## Acceptance criteria

`node --test --test-name-pattern="increment advances"` exits successfully.

## Verification

Run `node --test --test-name-pattern="increment advances"` and preserve command evidence.
